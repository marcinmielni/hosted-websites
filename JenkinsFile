pipeline {

    agent any

    stages {

        stage("build") {
            steps {
                git checkout mm_site_prod
                cd marcin.mielnicz.uk
                hugo -d
            }
        }
        stage("test") {
            steps {
               echo "dummy testing XD"
            }
        }
        stage("deploy") {
            steps {
                sh deploy.sh
            }
        }
    }
}
